meta:
  environment: node
  toolchain:
    name: npm
    version: 22
build:
  skip: false
  artifacts:
    '*': /
run:
  scriptName: index.js
  command: npx nx run api:serve
  persistenceMount: /data
  containerPort: 80
